fn main() {
    println!("Hello, world!");
    // This is calling a Rust macro, which is how metaprogramming is done in Rust.
    // If it were a function instead, it would look like this: println().
}

// If you see a !, you're calling a macro instead of a normal function.